import { Typography } from '@mui/material';

const Home = () => {
    return (
        <>
            <Typography variant="h3">
                Cosmetics Shop
            </Typography>

            <Typography sx={{ mt: 2 }}>
                Modern cosmetics store built with React and PHP.
            </Typography>
        </>
    );
};

export default Home;