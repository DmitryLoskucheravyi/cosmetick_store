import { Typography } from '@mui/material';

const UserOrders = () => {

    return (
        <Typography variant="h4">
            My Orders
        </Typography>
    );
};

export default UserOrders;